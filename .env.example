# Toggle the invite-only gate on/off
NUXT_PUBLIC_INVITE_ONLY=true

# Secret for signing invite session cookies (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
NUXT_INVITE_SECRET=
